# @lenso/tokens

Generated semantic tokens for Lenso UI. The DTCG `src/foundation.json`, `src/semantic.json`, `src/themes/*.json`, and `src/lenso.resolver.json` documents are authoritative. `src/tokens.json`, TypeScript, StyleX, CSS, and contract artifacts are generated and must not be edited directly.
