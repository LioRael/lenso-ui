# Lenso registry setup

Registry components use StyleX source and the public `@lenso/tokens` semantic contract. Configure the StyleX compiler in the Consumer application and import `@lenso/tokens/styles.css` once before adding components.
