---
title: "Tabs"
description: "A compact peer-view switcher with keyboard navigation and consumer-owned panels."
eyebrow: "Foundation component · Navigation"
metadata:
  - "Figma canonical"
  - "Implementation ready"
---

import { CodeExample } from "@/components/docs/code-example";
import { Guidance, GuidanceBlock } from "@/components/docs/guidance";
import { Playground } from "@/components/docs/playground";

<Playground id="tabs" />

  <Guidance>
    <GuidanceBlock title="Choose Tabs when">
      <ul>
        <li>Use tabs for peer views within one context, not for primary navigation.</li>
        <li>Use the default treatment for local content tabs. Apply <code>density="compact"</code> to both List and Tab for a 32px workspace/header switcher with a quiet filled selection.</li>
        <li>Keep labels short; the product shell handles horizontal overflow rather than shrinking tabs until they are unreadable.</li>
      </ul>
    </GuidanceBlock>
    <GuidanceBlock title="Accessibility">
      <ul>
        <li>Give every tab list an accessible name that describes the switched views.</li>
        <li>Base UI owns roles, roving focus, arrow keys, selection, and panel linkage.</li>
      </ul>
    </GuidanceBlock>
  </Guidance>

  <CodeExample
    code={String.raw`import { Tabs } from "@lenso/ui/tabs"

<Tabs.Root defaultValue="overview">
  <Tabs.List aria-label="Project sections" density="compact">
    <Tabs.Tab density="compact" value="overview">Overview</Tabs.Tab>
    <Tabs.Tab density="compact" value="documents">Documents</Tabs.Tab>
    <Tabs.Tab density="compact" value="members">Members</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel value="overview">Overview content</Tabs.Panel>
</Tabs.Root>`}
    description="Compact is an explicit navigation variant; the default remains suitable for local content tabs."
  />
