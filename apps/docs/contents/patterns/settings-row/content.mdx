---
title: "Settings Row"
description: "A consistent preference row that composes Select, Switch, Button, or consumer-owned controls."
eyebrow: "Product component · Forms"
metadata:
  - "Figma canonical"
  - "Implementation ready"
---

import { CodeExample } from "@/components/docs/code-example";
import { Guidance, GuidanceBlock } from "@/components/docs/guidance";
import { Playground } from "@/components/docs/playground";

<Playground id="settings-row" />

  <Guidance>
    <GuidanceBlock title="Usage guidance">
      <ul>
        <li>Use Switch for immediate binary preferences and Select for bounded choices.</li>
        <li>Use the Action slot for secondary configuration flows.</li>
        <li>Keep Title for layout-only rows; use Label when the title should activate a control.</li>
      </ul>
    </GuidanceBlock>
    <GuidanceBlock title="Accessibility">
      <ul>
        <li>
          Label and Control share generated IDs, so the visible label can name and natively activate
          a labelable trailing control.
        </li>
        <li>Pass disabled state to both the row and its interactive control.</li>
      </ul>
    </GuidanceBlock>
  </Guidance>

  <CodeExample
    code={String.raw`import { SettingsRow } from "@lenso/ui/settings-row"

<SettingsRow.Root>
  <SettingsRow.Copy>
    <SettingsRow.Title>Setting title</SettingsRow.Title>
    <SettingsRow.Description>Supporting description.</SettingsRow.Description>
  </SettingsRow.Copy>
  <SettingsRow.Control>{control}</SettingsRow.Control>
</SettingsRow.Root>`}
    description="The row owns layout only; existing controls retain their native Base UI semantics and behavior."
  />

  <CodeExample
    code={String.raw`import { SettingsRow } from "@lenso/ui/settings-row"
import { Switch } from "@lenso/ui/switch"

<SettingsRow.Root>
  <SettingsRow.Copy>
    <SettingsRow.Label>Allow background tasks</SettingsRow.Label>
    <SettingsRow.Description>
      Continue work after closing this view.
    </SettingsRow.Description>
  </SettingsRow.Copy>
  <SettingsRow.Control>
    {({ controlId, disabled, labelId, visualState }) => (
      <Switch.Root
        aria-labelledby={labelId}
        data-visual-state={visualState}
        disabled={disabled}
        id={controlId}
        layout="control-only"
      >
        <Switch.Thumb />
      </Switch.Root>
    )}
  </SettingsRow.Control>
</SettingsRow.Root>`}
    description="Label uses native label activation. Control exposes the generated IDs and coordinated hover state without DOM lookup or dataset mutation."
  />
