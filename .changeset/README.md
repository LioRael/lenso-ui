# Changesets

Every public API, semantic token, registry item, or material visual change needs a changeset. The public packages release as one fixed group and the release pipeline binds the registry snapshot to the same version.
